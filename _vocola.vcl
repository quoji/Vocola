# Global voice commands
include Unimacro.vch;

OK = {enter};
Cancel = {esc};

Save = {ctrl+s};

goto middle = MouseGrid(2,5) ButtonClick(1,1);

dictate anything <_anything> = $1;

Close = {alt+F4};

Switch = {alt +tab};


Chrome = WINKEY(1);

Sublime = WINKEY(2);


