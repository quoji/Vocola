include Unimacro.vch;
# Voice commands for chrome

New tab = {ctrl + T};

Close tab = {ctrl+w};

Vimium = {f};

Previous = {ctrl + shift +tab};
Previous 1..10 = {ctrl+shift+tab_$1};
Next = {ctrl + tab};
Refresh = {ctrl+r};